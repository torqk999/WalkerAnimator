//using Sandbox.ModAPI;

namespace IngameScript
{
    partial class Program
    {
        enum PARAM
        {
            Name = 0,
            TAG = 1,

            uIX = 2, // unique index (my)
            pIX = 3, // parent
            fIX = 4, // foot
            lIX = 5, // lock
            sIX = 6, // sync

            SettingInit = 4,
            //GroupName = 4,
            GripDirection = 5,
        }

    }
}
